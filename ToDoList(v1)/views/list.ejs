<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        *{
            margin: 0px;
            padding: 0px;
        }
        body{
            background-color: blueviolet;
        }
        .container{
             margin: 0 auto 0 auto;
             margin-top: 10%;
             padding: 30px;
             height: auto;
             width : 480px;
             border-radius: 10px;
             background-color: white;
        }
        .date_container{
            text-align: center;
        }
        .form_container{
            text-align: center;
        }
        .add_list{
            border:3px solid purple;
            padding: 5px;
            border-radius: 30px;
            text-align:right;
        }
        .add_list:focus{
            outline: none;
        }
        .btn_1{
            margin-left:5px;
            color: black;
            font-family: Copperplate, fantasy;
            padding:15px;
            padding-top: 5px;
            padding-bottom: 5px;
            border-radius:30px;
            box-shadow: 2px -1px 2px 2px rgb(53, 45, 45);
        }
    </style>
    <title>To Do List</title>
</head>
<body>
<div class="container">
    <div class="date_container">
        <h1><%= kindOfday%></h1>
    </div>
    <div class="form_container">
        <ul style="list-style-type:none;">
            <%for(var i=0; i<=addItems.length;i++){%>
                <li><%=addItems[i]%></li>
            <%}%>
        </ul>
        <form action="/" method="post">
            <input type="text" name="add_item" class="add_list" placeholder="What to Do?">
            <button type="submit"  name="Add" class="btn_1">ADD</button>
        </form>
    </div>
</div>    
    
</body>
</html>